openapi: "3.0.1"
info:
  title: Micro model service for text summarization
  version: 1.0.0
  description: It will use a pretrained bert
servers:
  - url: http://localhost:5080
  - url: https://api.5elements.ai/text-summarization/api/1.0
paths:
  /ping:
    get:
      operationId: ping
      responses:
        200:
          description: It will return a pong when everything if correct
  /info:
    get:
      operationId: info
      responses:
        200:
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Info"
  /definition:
    get:
      operationId: definition
      responses:
        200:
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Definition"
  /predict:
    post:
      operationId: predict
      requestBody:
        description: ""
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Input"
      responses:
        200:
          description: ""
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Output"
components:
  schemas:
    Info:
      type: object
      properties:
        version:
          type: string
        project:
          type: string
        service:
          type: string
        language:
          type: string
        type:
          type: string
        date:
          type: string
          format: date-time
    Definition:
      type: object
    Input:
      type: object
      properties:
        context:
          type: string
          description: The text you want to summarize
        config:
          type:  object
          description: The configs you can submit
          properties: 
            maxlength:
              type: string
              description: The length you want the summarization to be
    Output:
      type: object
      properties:
        answer:
          type: string
